<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Coarse only, and never required: a sighting without a location is still a sighting. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!--
        Without this, MediaStore hands out photographs with their location tags stripped, so a
        picture chosen from the gallery is filed at wherever the phone is standing now rather
        than where it was taken. See Gallery.coordinatesOf.
    -->
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <!--
        Reading the camera roll ourselves, so the grid can show DCIM rather than every image on
        the device. Refusing it costs the grid, not the feature: the system photo picker needs
        no permission and stays as the fallback. See PhotoLibrary.
    -->
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        android:minSdkVersion="33" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="Life List"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LifeList">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
